Cerate a sample_images named folder before executing in the same project directory. 
collection.ipynb is for taking the inputs as facial images for comparison. 
matching.ipynb is to actually match the faces. Change the confidence score as per the needs for matching percentage required. 

cheers!!
